<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Pseudokrat braucht keine speziellen Berechtigungen über die
         Standard-Sandbox hinaus. Hardened Runtime ist via
         "--options runtime" beim codesign ohnehin aktiv. -->

    <!-- Lockern, damit PyInstaller-Bootloader nicht von Sicherheitscheck
         erschlagen wird (JIT-freier Python-Loader). -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
